version: "3"

services:
  postgres_test:
    image: postgres:13
    environment:
      POSTGRES_USER: test
      POSTGRES_PASSWORD: test
#      POSTGRES_DB: test
    volumes:
      - test-data:/var/lib/postgresql/data
    ports:
      - "7432:5432"
volumes:
  test-data:
